version: '3.8'
services:
  api:
    build: ./server
    ports:
      - "3000:3000"
    volumes:
      - ./server:/app
    env_file:
      - ./server/.env
    environment:
      - NODE_ENV=development
      - PORT=3000
    restart: unless-stopped
